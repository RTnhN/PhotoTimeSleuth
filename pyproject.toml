[project]
name = "phototimesleuth"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9,<4.0"
dependencies = [
    "flask>=3.1.0",
    "piexif>=1.1.3",
    "ruff>=0.9.3",
]

[project.scripts]
PhotoTimeSleuth = "PhotoTimeSleuth.app:main"
